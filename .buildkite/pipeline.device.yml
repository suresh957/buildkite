# .buildkite/pipeline.device.yml
steps:  
  - label: Flash Unit
    concurrency: 1
    concurrency_group: "link-iot-server"
    agents:
      # Target an agent which has been started like:
      # buildkite-agent start --tags cartlink=cartlink-1
      cartlink: $CARTLINK
    command: /bin/bash /home/pi/.buildkite-agent/builds/raspberrypi-1/techwing/cartlink-flash/.buildkite/build.sh
